STAGE 3 — SONGPLAN SERIALIZATION

Use the frozen DecisionTrace, frozen Stage-2 musical material and the exact authoritative SongPlanV2 contract supplied by the runner. Serialize the already-decided material into one SongPlanV2 object.

Do not reconsider harmony, form, motifs or N3-P. Do not add or remove musical content for artistic reasons. Use only contract-supported fields, values and operations. Return only the SongPlanV2 object. Deterministic validation will decide whether it is valid; do not claim that it is valid in prose.

SERIALIZATION REQUIREMENT (technical clarification only):
- Return exactly ONE SongPlanV2 JSON object.
- Return raw JSON only.
- Do NOT use Markdown or code fences.
- Do NOT include prose before or after the JSON.
- Do NOT explain the answer.
- Do NOT ask follow-up questions.
- All fields required by SongPlanV2 must be present.
- Do not invent an alternate SongPlan representation.

CONTRACT PATH CLARIFICATION (technical only):
- In `arrangement.sections`, the required identifier field is `id`.
- `section_id` is used only by harmony assignments and track section assignments.
- `drums` tracks use `drum_voice` events and do not require `kit_id` or `map_id` in the SongPlan track. Host validation supplies the approved DrumExternalMap.
- `percussion` tracks require `kit_id` and `map_id` at the direct track-object path; their events use `instrument` and `sounding_articulation`.
- `time_signature` is a non-empty string such as `"4/4"`; object forms are invalid.
- `effect` events use only `bar`, `beat`, `duration`, and optional `id`; `position` is unsupported.
- Pitched events must use pitches in the declared scale unless `chromatic: true` is explicitly present.
- Unknown fields and duplicate JSON object keys are invalid.
This clarification does not add musical content or change SongPlanV2 semantics.


DURATION REPRESENTATION (technical clarification; SongPlanV2 semantics unchanged):
- Every event `duration` and `beat` is a JSON string parsed by music-engine 4.0.0 as Python Fraction-compatible text. Accepted lexical forms include integer, fraction `numerator/denominator`, decimal, and scientific notation; underscores within digits and outer whitespace follow the engine parser. Do not include whitespace around the fraction slash.
- Duration must be mathematically greater than zero; denominator must be nonzero. Beat position must be at least 1. The real engine validator is authoritative.
- Do not duplicate any JSON object key.

PITCH / HOST BOUNDARY (technical clarification):
- Keep the frozen pitches, tonic, mode, and chromatic intent unchanged. Every pitched note outside the declared scale must carry the already-authorized explicit `chromatic: true`; do not add that flag or alter pitches as a serialization repair. If the frozen material cannot be represented consistently, report the unresolved contract diagnostic.
- For `drums`, use `drum_voice`; host resolution uses the supplied approved DrumExternalMap identifier `song001_r1_steven_slate_map`.
- For `percussion`, preserve the model-selected `kit_id` and `map_id` exactly. No PercussionMap identifiers are approved for XMODEL-011. If percussion output requires a map, do not substitute the drums map or invent identifiers; deterministic host validation must block pending Owner-approved configuration.
